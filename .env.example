# Copy to .env and fill in. Never commit .env (it's gitignored).
#
# Neon Postgres: create a project at https://neon.tech, copy the connection string.
# Use the POOLED connection string for the app (host has "-pooler"),
# and the DIRECT (non-pooled) string for migrations if Neon gives you one.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"

# Optional: direct (non-pooled) URL for `prisma migrate`. Wire into prisma.config.ts if used.
# DIRECT_URL="postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"

# ─── Auth.js (NextAuth v5) ───
# Generate a secret: `npx auth secret`  (or: openssl rand -base64 33)
AUTH_SECRET=""

# Primary login is email/password (no env needed — works out of the box).
# Google OAuth is SECONDARY and OPTIONAL: the app runs without these, the
# Google button just won't authenticate until they're set.
# Client: https://console.cloud.google.com/apis/credentials
# Authorized redirect URI (dev): http://localhost:3000/api/auth/callback/google
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# Offline PWA mode (Legend-State + service worker). "1" enables; off by default.
NEXT_PUBLIC_OFFLINE_MODE=0

# ─── SMTP (self-checkout order receipts) ───
# Used by src/lib/mailer.ts to email the customer their order receipt.
# Any SMTP provider works (Brevo, Mailtrap, Resend SMTP, Gmail app password, ...).
# If unset, receipt emails are logged to the console instead of sent (dev-safe).
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
# "From" header for outgoing receipt emails, e.g. "Odoo Cafe <no-reply@cafe.com>"
SMTP_FROM="Odoo Cafe <no-reply@cafe.com>"
